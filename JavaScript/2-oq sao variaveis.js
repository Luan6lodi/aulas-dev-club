/*
📦 Exemplo prático
let nome = "Luan";   // guardando um texto
let idade = 20;      // guardando um número
let gostaDeF1 = true; // guardando um valor booleano (verdadeiro ou falso)

console.log(nome);   // vai mostrar: Luan
console.log(idade);  // vai mostrar: 20
console.log(gostaDeF1); // vai mostrar: true

🔑 Palavras-chave para criar variáveis:

var → jeito antigo, quase não se usa mais.

let → jeito moderno, valor pode mudar depois.

const → valor não muda (constante).

📝 Resumindo:

Variável = um espaço de memória para guardar valores.

Pode guardar qualquer tipo de dado.

Criamos usando let, const (ou var, mas não é recomendado).

*/

let numeroDeAlunosDaSalaSete = 40

console.log(numeroDeAlunosDaSalaSete)